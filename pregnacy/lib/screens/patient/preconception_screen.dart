import 'package:flutter/material.dart';

/// Cycle tracking and wellness tools for the preconception stage.
class PreconceptionScreen extends StatelessWidget {
  const PreconceptionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Preconception Tracker')),
      body: const Center(
        // TODO: cycle tracking, ovulation prediction, wellness tips
        child: Text('Preconception Tracker — Coming Soon'),
      ),
    );
  }
}
